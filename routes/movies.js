const express = require("express");
const router = express.Router();
const movies = require('./moveis/model/moviemodel');
const {number1, number2, number3, number4} = require ('./movies/controller/moviecontroller')